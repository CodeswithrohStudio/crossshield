{
  "name": "crossshield",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:westend": "ts-node scripts/deploy-westend.ts",
    "deploy:moonbase": "hardhat run scripts/deploy.ts --network moonbase",
    "seed": "hardhat run scripts/seed-oracle.ts --network localhost",
    "seed:westend": "ts-node scripts/seed-westend.ts",
    "seed:moonbase": "hardhat run scripts/seed-oracle.ts --network moonbase",
    "node": "hardhat node",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}
